---
description: 'Iterative quality assurance process using the QA Loop — plan, execute, verify, repeat'
agent: "QA-orchestrator"
argument-hint: "Task ID, suite name, and objective"
---

If no text prompt is provided with the `/qa` command, use the chat context (previous messages) to determine the implicit request and respond accordingly. If a prompt is provided, respond directly to it.

${input:Describe the QA task or process to execute}
